      tasks:
        stylecop_plugin_sessions:
          timeout: 300
          name: Style checks Sessions plugin
          
          trial-attachments:
            logs:
             include-match: ^attachment[\w\d\-\_]*\.log$
             content-type: text/plain
          
          scripts:
            run_nuget_restore:
              body: |
                start /w .nuget\nuget.exe restore
                echo nuget.exe error level is %ERRORLEVEL%
                exit /b %ERRORLEVEL%

            build_plugin_sessions:  
              ignore-state: false
              start-when:
                - script: run_nuget_restore   
                  states: [passed]  
              body: |
                start /w msbuild Plugin_Sessions\Plugin_Sessions.csproj /t:Build /p:Configuration="Debug" /p:Platform="Mixed Platforms" /p:OutputPath=bin\Debug /flp:Summary;Verbosity=minimal;LogFile=attachment-msbuild-info_plugin_sessions.log /flp1:logfile=attachment-msbuild-error_plugin_sessions.log;errorsonly /flp2:logfile=attachment-msbuild-warning_plugin_sessions.log;warningsonly
                exit /b %ERRORLEVEL%

            # Evaluate outputfiles, check for StyleCop records
            codingconvenctions_plugin_sessions:
              ignore-state: false
              start-when:
                - script: build_plugin_sessions   
                  states: [passed]                
              body: |
                if not exist "attachment-msbuild-warning_plugin_sessions.log" exit /b 1
                type "attachment-msbuild-warning_plugin_sessions.log" | find /c "warning : SA" > temp_sessions.txt    
                set /p VAR=<temp_sessions.txt
                del temp_sessions.txt
                if %VAR% gtr 0 exit /b 2
                exit /b 0
                