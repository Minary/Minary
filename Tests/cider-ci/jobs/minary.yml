  minary_tests:

    name: 'Minary tests'
    description: Minary tests 
    run-on: 
      - type: branch
        include-match: ^development$

    context:
      task-defaults:
        max-auto-trials: 2
        traits:
          windows: true
 
      _cider-ci_include:
        - tests/cider-ci/tasks/minary/attack_service.yml